kafka:
  bootstrapAddress: ${KAFKA_BOOTSTRAP_SERVERS}
  schema:
    registry:
      url: ${KAFKA_SCHEMA_REGISTRY_URL}

spring:
  data:
    mongodb:
      uri: mongodb://${MONGO_HOST}:${MONGO_PORT}/${MONGO_REPLICA_SET}